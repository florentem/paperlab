name: PaperLab
version: '${version}'
main: paperlab.PaperLabPlugin
api-version: '26.2'
description: Technical toolset for Minecraft farm research on Paper 26.2
authors: [ oCreker ]

# Both the core's own bots (/player) and the external FakePlayer-CE are supported.
# /log mobcaps and /ghost work with any bot, since they are ordinary ServerPlayers.

dependencies:
  server:
    LitematicaFolia:
      load: BEFORE
      required: false
    TAB:
      load: BEFORE
      required: false
