name: PaperLab
version: '${version}'
main: paperlab.PaperLabPlugin
api-version: '26.2'
description: Технический инструментарий для исследования ферм на Paper 26.2
authors: [ oCreker ]

# Поддерживаются как встроенные боты ядра (/player), так и внешний FakePlayer-CE.
# /log mobcaps и /ghost работают с любыми ботами (они обычные ServerPlayer).

dependencies:
  server:
    LitematicaFolia:
      load: BEFORE
      required: false
    TAB:
      load: BEFORE
      required: false
